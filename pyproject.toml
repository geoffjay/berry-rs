[project]
name = "berry-docs"
version = "0.4.0"
description = "Documentation for Berry - A semantic memory system for AI assistants"
readme = "README.md"
requires-python = ">=3.12.2"
dependencies = ["zensical>=0.0.20"]
